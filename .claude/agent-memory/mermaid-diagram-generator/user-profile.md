---
name: user-profile
description: User's technical background and domain interests inferred from diagram requests
metadata:
  type: user
---

User works across both technical and talent development domains. Demonstrated technical depth: RAG pipeline diagram requests use precise ML vocabulary (vector embeddings, vector store, top-K retrieval, LLM, document chunking, ingestion pipeline). Also operates in instructional design/organizational learning (ATD Continuous Performance Improvement model). Consistently requests subgraph grouping to separate logical concerns within a single flowchart. Diagrams are intended for documentation/README use — requests output written to specific .md files with a title and mermaid code fence. Prefers `flowchart TD` direction. Uses stadium nodes `([...])` for user-facing endpoints and cylinder nodes `[(...)]` for data stores. Expects cross-subgraph edges to be labeled explicitly.
