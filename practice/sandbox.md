# Git practice sandbox

Edit this file, then try these commands from the repo root:

```powershell
git status
git diff
git add practice/sandbox.md
git commit -m "Update practice notes"
git log --oneline -5
```

## Practice checklist

- [ ] Change a line below and run `git diff`
- [ ] Stage with `git add` and check `git status`
- [ ] Commit with your own message
- [ ] Try Claude Code: `/commit-code` (uses `.claude/commands/commit-code.md`)
- [ ] Try Claude Code: `/dad-joke programming`

## Scratch area (edit freely)

Today's goal: learn custom commands and git basics in claude crash course.

Favorite editor: Cursor

Random note: Replace this line with anything you want.
